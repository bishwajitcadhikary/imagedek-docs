# Requirements
The Laravel framework has a few system requirements. You should ensure that your web server has the following minimum PHP version and extensions:
* PHP >= 8.1
* Ctype PHP Extension
* cURL PHP Extension
* DOM PHP Extension
* Fileinfo PHP Extension
* Filter PHP Extension
* Hash PHP Extension
* Mbstring PHP Extension
* OpenSSL PHP Extension
* PCRE PHP Extension
* PDO PHP Extension
* PDOMySQL PHP Extension
* Session PHP Extension
* Tokenizer PHP Extension
* XML PHP Extension
* XMLWriter PHP Extension
* JSON PHP Extension
* GD PHP Extension

## Installation
### Installation Service
1. Shared Hosting Installation Service ($25)
2. VPS Installation Service ($50)
3. Install ImageDek on your own server ($100)

#### Hire us: [Visit](https://wrapbootstrap.com/user/kindigi/message) or [Mail](mailto:bishwajitcadhikary@gmail.com)

### Installation on Shared Hosting
#### Step 1: Download & Upload
Download the latest version of ImageDek from your WrapBootstrap account and unzip it to your computer.
1. imagedek.zip
Upload the imagedek.zip file to your server using an FTP client or File Manager.

#### Step 2: File Setup
##### 1. Upload the imagedek.zip file to your server
1. Login to your cPanel account.
2. Click on the File Manager icon.
3. Click on the public_html folder.
4. Click on the Upload button.
5. Click on the Choose File button.
6. Select the imagedek.zip file from your computer.

##### 2. Extract the imagedek.zip file
1. Click on the imagedek.zip file.
2. Click on the Extract button.
3. Delete the imagedek.zip file.

#### Step 3: Database Setup
##### 1. Create a database
1. Go to your cPanel account.
2. Click on the MySQL® Database Wizard.
3. Give a name to your database.
4. Note: Remember to write down your database name, username, and password. You will need them later.
5. Click on the Next Step.

##### 2. Create a user
1. Create a username and password for your database.
2. Click on the Create User.

##### 3. Grant privileges
1. Now, click on All Privileges button.
2. Click on the Make Changes button.

##### 4. Done
🥳🎉Congratulations! You have successfully created a database. 🎉🥳
Note: Remember to write down your database name, username, and password. You will need them later.

#### Step 4: Configure .htaccess file
1. Go to your cPanel account.
2. Click on the File Manager icon.
3. Click on the public_html folder.
4. Click on the .htaccess file.
5. Click on the Edit button.
6. Replace the code with the following code:
7. Click on the Save Changes button.
```apache copy
<IfModule mod_rewrite.c>
    Options +FollowSymLinks
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ /public/$1
    RewriteRule ^(/)?$ public/$1 [L]
</IfModule>
```

***Optional:*** If you are using nginx, you can use the following code:
```nginx copy
server {
    listen 80;
    listen [::]:80;
    server_name example.com;
    root /path/to/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    # Your existing location blocks...
}
```
🥳🎉Congratulations! You have successfully configured the .htaccess file. 🎉🥳

#### Step 5: Installation Wizard
##### 1. Open your browser and go to your domain name.
For example: [http://yourdomain.com/install](http://yourdomain.com/install)
##### 2. Server Requirements
1. Click on the `Check Requirements`button.
2. If you see the green tick mark, it means that your server meets all the requirements. <br/>

##### 3. Folder Permission
1. Click on the `Check Folder Permission` button.
2. If you see the green tick mark, it means that file permissions are set correctly.
But if you see the red cross mark, it means that file permissions are not set correctly.
Please visit the following link to set the file permissions correctly: [Troubleshooting](/troubleshooting/folder)
3. Click on the `Database button`.

##### 4. Database Setup
1. Fill in the database details that you have created in Step 3.
2. Click on the `Save & Continue` button.

##### 5. Admin Setup
1. Now, fill in the admin details.
2. Click on the `Save & Continue` button.
🥳🎉Congratulations! You have successfully installed the ImageDek.